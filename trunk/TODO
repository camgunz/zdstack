- Debug with multiple players, and then multiple servers and multiple players
- Test with MySQL and PostgreSQL
- ZDSConfigParser stuff
  - Use a sorted dict class for _sections
  - Add a getlist(section, option, parse_func=None)
- Make sure non-debugging output is quiet
- Add 'save log' option
- Add rapid-restart detection
- Test all RPC functions
- Deadlock in players/teams lists and EventHandler (sort this out)
